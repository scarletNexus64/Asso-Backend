@extends('admin.layouts.app')
@section('title', 'Modifier Credential')
@section('content')
<div class="p-6"><div class="max-w-4xl mx-auto"><div class="bg-dark-100 border border-dark-200 rounded-lg p-6"><h1 class="text-2xl font-bold text-white mb-6">Modifier le credential</h1><form action="{{ route('admin.vault.update', $vault) }}" method="POST">@csrf @method('PUT')<div class="space-y-6"><div><label class="block text-gray-300 mb-2">Nom *</label><input type="text" name="name" value="{{ $vault->name }}" required class="w-full px-4 py-2 bg-dark-50 border border-dark-300 rounded-lg text-white"></div><div><label class="block text-gray-300 mb-2">Type *</label><select name="type" class="w-full px-4 py-2 bg-dark-50 border border-dark-300 rounded-lg text-white"><option value="password" {{ $vault->type == 'password' ? 'selected' : '' }}>Mot de passe</option><option value="api_key" {{ $vault->type == 'api_key' ? 'selected' : '' }}>Clé API</option><option value="token" {{ $vault->type == 'token' ? 'selected' : '' }}>Token</option><option value="ssh_key" {{ $vault->type == 'ssh_key' ? 'selected' : '' }}>Clé SSH</option><option value="other" {{ $vault->type == 'other' ? 'selected' : '' }}>Autre</option></select></div><div><label class="block text-gray-300 mb-2">Catégorie</label><select name="category_id" class="w-full px-4 py-2 bg-dark-50 border border-dark-300 rounded-lg text-white"><option value="">Aucune</option>@foreach($categories as $cat)<option value="{{ $cat->id }}" {{ $vault->category_id == $cat->id ? 'selected' : '' }}>{{ $cat->name }}</option>@endforeach</select></div><div><label class="block text-gray-300 mb-2">Username</label><input type="text" name="username" value="{{ $vault->username }}" class="w-full px-4 py-2 bg-dark-50 border border-dark-300 rounded-lg text-white"></div><div><label class="block text-gray-300 mb-2">Password (laisser vide pour ne pas changer)</label><input type="password" name="password" class="w-full px-4 py-2 bg-dark-50 border border-dark-300 rounded-lg text-white"></div><div><label class="block text-gray-300 mb-2">URL</label><input type="url" name="url" value="{{ $vault->url }}" class="w-full px-4 py-2 bg-dark-50 border border-dark-300 rounded-lg text-white"></div><div><label class="block text-gray-300 mb-2">Notes</label><textarea name="notes" rows="4" class="w-full px-4 py-2 bg-dark-50 border border-dark-300 rounded-lg text-white">{{ $vault->notes }}</textarea></div><div class="flex gap-3"><button type="submit" class="px-6 py-2 bg-primary-600 hover:bg-primary-700 text-white rounded-lg">Mettre à jour</button><a href="{{ route('admin.vault.index') }}" class="px-6 py-2 bg-dark-300 hover:bg-dark-400 text-white rounded-lg">Annuler</a></div></div></form></div></div></div>
@endsection
